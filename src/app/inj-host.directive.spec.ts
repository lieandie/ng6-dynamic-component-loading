import { InjHostDirective } from './inj-host.directive';

describe('InjHostDirective', () => {
  it('should create an instance', () => {
    const directive = new InjHostDirective();
    expect(directive).toBeTruthy();
  });
});
